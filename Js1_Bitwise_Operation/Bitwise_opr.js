function bitwiseAND(n1, n2) {
    console.log(`Binary value of n1 :- ${n1.toString(2)}`);
    console.log(`Binary value of n1 :- ${n2.toString(2)}`);
    console.log(`Bitwise AND of ${n1} AND ${n2} in Binary is ${(n1 & n2).toString(2)} And in Decimal is ${n1 & n2}`);

    //return (n1 & n2);
}

function bitwiseOR(n1, n2) {
    console.log(`Binary value of n1 :- ${n1.toString(2)}`);
    console.log(`Binary value of n1 :- ${n2.toString(2)}`);
    console.log(`Bitwise OR of ${n1} AND ${n2} in Binary is ${(n1 | n2).toString(2)} And in Decimal is ${n1 | n2}`);

    //return (n1 & n2);
}

function bitwiseXor(n1, n2) {
    console.log(`Binary value of n1 :- ${n1.toString(2)}`);
    console.log(`Binary value of n1 :- ${n2.toString(2)}`);
    console.log(`Bitwise XOR of ${n1} AND ${n2} in Binary is ${(n1 ^ n2).toString(2)} And in Decimal is ${n1 ^ n2}`);

    // return (n1 & n2);
}